// Placeholder — replaced by the real shell (see docs/PLAN.md §UI).
export default function App() {
  return (
    <main className="min-h-screen bg-paper text-ink p-8">
      <h1 className="text-2xl font-semibold">DegreeLume Assistant</h1>
      <p className="text-muted mt-2">Scaffold. The UI lands in src/components.</p>
    </main>
  )
}
